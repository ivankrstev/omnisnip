{
  "name": "omnisnip",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter \"./apps/*\" --parallel dev",
    "dev:cli": "pnpm --filter @omnisnip/cli dev",
    "build": "pnpm --filter \"./apps/*\" --parallel build",
    "build:cli": "pnpm --filter @omnisnip/cli build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "type-check": "tsc --noEmit"
  },
  "author": "Ivan Krstev",
  "license": "ISC",
  "packageManager": "pnpm@10.25.0",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@vitest/ui": "^4.0.16",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "engines": {
    "node": ">=24.0.0 <25",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  }
}
